# Vertice POS - Configuración del Frontend
# Copia este archivo a .env

# URL del servidor API (cambia la IP a la del servidor)
VITE_API_URL=http://localhost:3000/api

# URL del servidor de impresión (cambia la IP a la del servidor)
VITE_PRINT_SERVER_URL=http://localhost:3001
